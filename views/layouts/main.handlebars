<html>
    <head>
        <title>{{title}}</title>
        <link rel="stylesheet" href="css/{{style}}">
    </head>
    <body>
        {{{body}}}
        {{#if script}}
        <script src="js/{{script}}"></script>
        {{/if}}
    </body>
</html>