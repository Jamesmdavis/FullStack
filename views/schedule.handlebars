<h1>Schedule</h1>

<div id="container">
    <div id="border">
        {{#each contacts}}
            <div class="element">
                <span>{{this.name}}</span>
                <span>{{this.email}}</span>
                <span>{{this.age}}</span>
            </div>
        {{/each}}
    </div>
</div>